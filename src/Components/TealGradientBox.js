import styled from 'styled-components'
import { Box } from 'rebass'

const TealGradientBox = styled(Box)`
    background-image: linear-gradient(to right, #17b0b0, #49dcdc);
`

export default TealGradientBox
